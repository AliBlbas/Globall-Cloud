/* Globall Cloud legacy compatibility stub.
 * The active Staff OS V5 surface provides these controls through native modules.
 */
(() => {
  'use strict';
  window.__gcUltimateControl = window.__gcUltimateControl || { legacy: true };
})();
