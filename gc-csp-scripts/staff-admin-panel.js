(() => {
  'use strict';
  const ORIGINS = new Set(['https://globall-cloud.pages.dev','https://globall-cloud.netlify.app']);
  const esc = (v) => String(v ?? '').replace(/[&<>"']/g, (m) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const css = `
    #gcStaffAdminLauncher{position:fixed;right:18px;bottom:18px;z-index:99999;border:1px solid rgba(111,224,255,.45);background:linear-gradient(135deg,#08233c,#0a5f73);color:#fff;border-radius:14px;padding:12px 16px;font-weight:800;cursor:pointer;box-shadow:0 12px 36px rgba(0,0,0,.28)}
    #gcStaffAdminOverlay{position:fixed;inset:0;background:rgba(2,8,18,.72);backdrop-filter:blur(10px);z-index:99998;display:none;padding:22px}
    #gcStaffAdminPanel{max-width:1100px;height:min(92vh,900px);margin:auto;background:#071425;color:#f4f8ff;border:1px solid rgba(111,224,255,.28);border-radius:24px;display:grid;grid-template-columns:300px 1fr;overflow:hidden;box-shadow:0 30px 80px rgba(0,0,0,.5)}
    .gcsa-side{border-right:1px solid rgba(255,255,255,.08);padding:18px;overflow:auto}.gcsa-main{padding:20px;overflow:auto}.gcsa-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:16px}.gcsa-title{font-size:20px;font-weight:900}.gcsa-sub{font-size:12px;opacity:.68;margin-top:4px}.gcsa-btn{border:1px solid rgba(255,255,255,.12);background:#0c2138;color:#fff;border-radius:10px;padding:9px 12px;cursor:pointer}.gcsa-btn.primary{background:linear-gradient(135deg,#0ea5e9,#10b981)}.gcsa-btn.danger{background:#4c1720}.gcsa-input,.gcsa-select{width:100%;box-sizing:border-box;background:#091c30;color:#fff;border:1px solid rgba(255,255,255,.12);border-radius:10px;padding:10px}.gcsa-list{display:grid;gap:8px;margin-top:12px}.gcsa-item{width:100%;text-align:left;color:#fff;background:#091b2f;border:1px solid rgba(255,255,255,.07);border-radius:12px;padding:11px;cursor:pointer}.gcsa-item:hover{border-color:rgba(111,224,255,.28)}.gcsa-item b{display:block}.gcsa-pill{display:inline-block;font-size:10px;padding:3px 7px;border-radius:999px;background:#12324a;margin-top:5px}.gcsa-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.gcsa-field label{display:block;font-size:12px;opacity:.75;margin-bottom:6px}.gcsa-field{margin-bottom:12px}.gcsa-full{grid-column:1/-1}.gcsa-actions{display:flex;gap:8px;flex-wrap:wrap}.gcsa-msg{margin:8px 0;padding:10px 12px;border-radius:10px;background:#0a2137;font-size:12px;display:none}.gcsa-card{background:#0a192b;border:1px solid rgba(255,255,255,.07);border-radius:16px;padding:16px}.gcsa-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin:12px 0}.gcsa-stat{background:#08182a;border:1px solid rgba(255,255,255,.06);padding:10px;border-radius:12px}.gcsa-stat strong{display:block;font-size:18px}.gcsa-stat span{font-size:10px;opacity:.6}.gcsa-note{font-size:11px;opacity:.58;margin-top:8px}
    @media(max-width:760px){#gcStaffAdminOverlay{padding:8px}#gcStaffAdminPanel{grid-template-columns:1fr;height:96vh}.gcsa-side{display:none}.gcsa-grid{grid-template-columns:1fr}.gcsa-stats{grid-template-columns:repeat(2,minmax(0,1fr))}}
  `;
  const mount = () => {
    if (document.getElementById('gcStaffAdminPanel')) return;
    const style = document.createElement('style'); style.textContent = css; document.head.appendChild(style);
    const launcher = document.createElement('button'); launcher.id='gcStaffAdminLauncher'; launcher.textContent='⚙️ Super Admin · Staff'; launcher.hidden=true;
    const overlay = document.createElement('div'); overlay.id='gcStaffAdminOverlay';
    overlay.innerHTML = `<div id="gcStaffAdminPanel"><aside class="gcsa-side"><div class="gcsa-head"><div><div class="gcsa-title">Staff</div><div class="gcsa-sub">One-query directory</div></div><button class="gcsa-btn" id="gcsaClose">×</button></div><input class="gcsa-input" id="gcsaSearch" placeholder="گەڕان..." autocomplete="off"><div class="gcsa-list" id="gcsaList"></div></aside><main class="gcsa-main"><div class="gcsa-head"><div><div class="gcsa-title">Super Admin Staff Control</div><div class="gcsa-sub">دروستکردن، دەستکاریکردن، چالاک/ناچالاککردن و reset ـی password.</div></div><div class="gcsa-actions"><button class="gcsa-btn" id="gcsaNew">+ Staff</button><button class="gcsa-btn" id="gcsaRefresh">↻</button></div></div><div class="gcsa-msg" id="gcsaMsg"></div><section class="gcsa-card"><div class="gcsa-stats"><div class="gcsa-stat"><strong id="gcsaShipments">0</strong><span>Assigned shipments</span></div><div class="gcsa-stat"><strong id="gcsaTasks">0</strong><span>Open tasks</span></div><div class="gcsa-stat"><strong id="gcsaCustomers">0</strong><span>Managed customers</span></div><div class="gcsa-stat"><strong id="gcsaActivity">0</strong><span>Activity events</span></div></div><div class="gcsa-grid"><div class="gcsa-field"><label>Full name</label><input class="gcsa-input" id="gcsaName"></div><div class="gcsa-field"><label>Email</label><input class="gcsa-input" id="gcsaEmail" type="email" autocomplete="off"></div><div class="gcsa-field"><label>Phone</label><input class="gcsa-input" id="gcsaPhone" autocomplete="off"></div><div class="gcsa-field"><label>Role</label><select class="gcsa-select" id="gcsaRole"><option value="staff">staff</option><option value="operations">operations</option><option value="warehouse">warehouse</option><option value="warehouse_china">warehouse_china</option><option value="warehouse_uae">warehouse_uae</option><option value="warehouse_erbil">warehouse_erbil</option><option value="driver">driver</option><option value="accountant">accountant</option><option value="finance">finance</option><option value="admin">admin</option><option value="super_admin">super_admin</option></select></div><div class="gcsa-field"><label>Branch</label><select class="gcsa-select" id="gcsaBranch"><option value="all">all</option><option value="erbil">erbil</option><option value="china">china</option><option value="dubai">dubai</option><option value="uae">uae</option><option value="usa">usa</option><option value="warehouse">warehouse</option></select></div><div class="gcsa-field"><label>Active</label><select class="gcsa-select" id="gcsaActive"><option value="true">ACTIVE</option><option value="false">INACTIVE</option></select></div><div class="gcsa-field gcsa-full"><label>New password / reset password (12+)</label><input class="gcsa-input" id="gcsaPassword" type="password" autocomplete="new-password" placeholder="بۆ دروستکردن/گۆڕین optional ـە"></div></div><div class="gcsa-actions" style="margin-top:10px"><button class="gcsa-btn primary" id="gcsaSave">پاشەکەوتکردن</button><button class="gcsa-btn" id="gcsaReset">Reset Password</button><button class="gcsa-btn danger" id="gcsaDeactivate">Set Active / Inactive</button></div><div class="gcsa-note">Super Admin خۆی ناتوانێت خۆی demote/deactivate بکات.</div></section></main></div>`;
    document.body.appendChild(launcher); document.body.appendChild(overlay);
    launcher.addEventListener('click',()=>overlay.style.display='block');
    overlay.querySelector('#gcsaClose').addEventListener('click',()=>overlay.style.display='none');
    overlay.addEventListener('click',(e)=>{if(e.target===overlay) overlay.style.display='none'});
    overlay.querySelector('#gcsaRefresh').addEventListener('click',()=>void loadDirectory());
    overlay.querySelector('#gcsaNew').addEventListener('click',()=>fill(null));
    overlay.querySelector('#gcsaSearch').addEventListener('input',()=>renderList());
    overlay.querySelector('#gcsaSave').addEventListener('click',()=>void save(false));
    overlay.querySelector('#gcsaReset').addEventListener('click',()=>void save(true));
    overlay.querySelector('#gcsaDeactivate').addEventListener('click',()=>void toggleActive());
    window.__gcStaffAdmin = {overlay,launcher};
  };
  let rows=[], selected=null;
  const api = async (path='', options={}) => {
    const client = window.gcEnsureSupabase ? await window.gcEnsureSupabase() : window.gcSupabase || window.sb;
    if (!client?.auth) throw new Error('Supabase client unavailable');
    const {data:sessionData} = await client.auth.getSession();
    const token = sessionData?.session?.access_token; if (!token) throw new Error('Login required');
    const res = await fetch(`${location.origin}/functions/v1/staff-directory${path}`, { ...options, headers:{...(options.headers||{}),Authorization:`Bearer ${token}`,apikey:client.supabaseKey||''}});
    const body = await res.json().catch(()=>({})); if (!res.ok) throw new Error(body.error || `Request failed (${res.status})`); return body;
  };
  const msg = (text) => {const el=document.getElementById('gcsaMsg'); if(!el)return; el.textContent=text; el.style.display='block'; clearTimeout(window.__gcsaMsgTimer); window.__gcsaMsgTimer=setTimeout(()=>el.style.display='none',5000)};
  const fill = (row) => { selected=row||null; const ids={Name:row?.full_name||'',Email:row?.email||'',Phone:row?.phone||'',Role:row?.role||'staff',Branch:row?.branch||'all',Active:String(row?.is_active!==false)}; document.getElementById('gcsaName').value=ids.Name; document.getElementById('gcsaEmail').value=ids.Email; document.getElementById('gcsaPhone').value=ids.Phone; document.getElementById('gcsaRole').value=ids.Role; document.getElementById('gcsaBranch').value=ids.Branch; document.getElementById('gcsaActive').value=ids.Active; document.getElementById('gcsaPassword').value=''; };
  const renderList = () => { const root=document.getElementById('gcsaList'); if(!root)return; const q=(document.getElementById('gcsaSearch').value||'').toLowerCase().trim(); const filtered=rows.filter(r=>`${r.full_name||''} ${r.email||''} ${r.role||''} ${r.branch||''}`.toLowerCase().includes(q)); root.innerHTML=filtered.map((r,i)=>`<button class="gcsa-item" data-i="${rows.indexOf(r)}"><b>${esc(r.full_name||r.email||'Unnamed')}</b><span>${esc(r.email||'—')}</span><span class="gcsa-pill">${esc(r.role||'—')} · ${esc(r.branch||'—')} · ${r.is_active?'ACTIVE':'INACTIVE'}</span></button>`).join(''); root.querySelectorAll('.gcsa-item').forEach(b=>b.addEventListener('click',()=>{fill(rows[Number(b.dataset.i)]);})) };
  const loadDirectory = async () => { try { mount(); const data=await api(); rows=data.items||[]; if(data.can_manage!==true){window.__gcStaffAdmin.launcher.hidden=true;return;} window.__gcStaffAdmin.launcher.hidden=false; renderList(); if(!selected && rows[0]) fill(rows[0]); const sum=rows.reduce((a,r)=>({ship:a.ship+Number(r.shipment_count||0),tasks:a.tasks+Number(r.open_task_count||0),customers:a.customers+Number(r.customer_count||0),activity:a.activity+Number(r.activity_count||0)}),{ship:0,tasks:0,customers:0,activity:0}); document.getElementById('gcsaShipments').textContent=sum.ship;document.getElementById('gcsaTasks').textContent=sum.tasks;document.getElementById('gcsaCustomers').textContent=sum.customers;document.getElementById('gcsaActivity').textContent=sum.activity;
    } catch(e){ console.warn('[Globall Cloud] staff admin:',e); } };
  const save = async (resetPassword) => { try { if(!selected && resetPassword) throw new Error('First select a staff member.'); const data={full_name:document.getElementById('gcsaName').value.trim(),email:document.getElementById('gcsaEmail').value.trim(),phone:document.getElementById('gcsaPhone').value.trim(),role:document.getElementById('gcsaRole').value,branch:document.getElementById('gcsaBranch').value}; const password=document.getElementById('gcsaPassword').value; if(resetPassword){ if(!password) throw new Error('Password is required.'); const out=await api('',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'reset_password',data:{staff_id:selected.id,password}})}); msg(out.ok?'Password reset done.':'Completed.'); }
      else if(selected){ data.staff_id=selected.id; data.is_active=document.getElementById('gcsaActive').value==='true'; const out=await api('',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'update_staff',data})}); msg(`Updated: ${out.staff?.full_name||selected.id}`); await loadDirectory(); }
      else { if(password)data.password=password; const out=await api('',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'create_staff',data})}); msg(`Created: ${out.staff?.full_name||''}${out.temporary_password?' · Temporary password shown now.':''}`); if(out.temporary_password) window.prompt('Temporary password — save it now:',out.temporary_password); fill(out.staff); await loadDirectory(); }
    } catch(e){msg(e.message||'Request failed')} };
  const toggleActive = async () => { if(!selected)return msg('Staff member هەڵبژێرە.'); const next=selected.is_active===false; const data={staff_id:selected.id,full_name:selected.full_name||'',email:selected.email||'',phone:selected.phone||'',role:selected.role||'staff',branch:selected.branch||'all',is_active:next}; try { const out=await api('',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'set_active',data})}); msg(`Status: ${next?'ACTIVE':'INACTIVE'}`); await loadDirectory(); fill(out.staff); } catch(e){msg(e.message||'Request failed')} };
  const init = async () => { try { await new Promise(r=>setTimeout(r,120)); mount(); await loadDirectory(); } catch(e){ console.warn('[Globall Cloud] staff admin init:',e); } };
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',()=>void init(),{once:true}); else void init();
})();
